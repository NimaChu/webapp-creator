interface:
  display_name: "WebApp Creator"
  short_description: "Create portable single-file HTML apps with optional local AI."
  default_prompt: "Use $webapp-creator to build a polished standalone HTML app that runs locally."
