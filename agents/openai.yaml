interface:
  display_name: "HTML Design"
  short_description: "Create portable single-file HTML apps with optional local AI."
  default_prompt: "Use $html-design to build a polished standalone HTML app that runs locally."
